<div class="container">
  <h1>Instituciones</h1>

    <% @institutions.each do |institution| %>
      <div class="card-index">
        <%= cl_image_tag institution.photo.key, height: 200, width: 200, crop: :fill %>
        <div class="card-product-infos mt-3 mb-3">
          <%= link_to institution.name, institution_path(institution.id)%>
        </div>
      </div>
    <% end %>

  <%= link_to 'Agregar una nueva institución', new_institution_path , class: "btn btn-ghost mt-3 ml-3"  %>

</div>
