<%= simple_form_for career do |f| %>
  <%= f.input :name %>
  <%= f.input :area %>
  <%= f.input :duration %>
  <%= f.input :profile %>
  <%= f.input :description %>

  <%= f.submit "Guardar", class: "btn btn-dark mb-3 mt-3" %>
<% end %>
