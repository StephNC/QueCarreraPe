<div id="container">
  <div class="banner eventos">
      <div class="row">
        <div class="col-md-12 text-center">
          <h1 class="m-0">Carreras</h1>
        </div>
      </div>
      <%= image_tag "wave1.png" %>
  </div>
</div>

<div class="container">
  <%= link_to 'Atrás', "/", class: "btn btn-ghost mb-2" %>
  <%= link_to 'Agregar una nueva carrera', new_career_path , class: "btn btn-ghost mb-2"  %>

  <div class="flex-wrap d-flex justify-content-between">
    <% @careers.each do |career| %>
      <div class="card-vertical d-flex" style="background-image:linear-gradient(rgba(223,218,248,0.05), rgba(0,0,0,0.6)), url( <%= cl_image_path(career.photo.key) %>)">
      <%= link_to career.name, career_path(career) %></h2>
      </div>
    <% end %>
  </div>
</div>
