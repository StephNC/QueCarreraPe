<div id="container">
  <div class="banner eventos">
      <div class="row">
        <div class="col-md-12 text-center">
         <h1 class="m-3 mt-3 text-center">Agregar una institución a <%= @career.name %></h1>
        </div>
      </div>
      <%= image_tag "wave1.png" %>
  </div>
</div>

<div class= "container">
  <div id="card_signup_create">
    <div class="card-new">
      <%= simple_form_for [@career, @career_institution] do |f| %>
        <%= f.input :institution_id, collection: Institution.all %>
        <%= f.submit "Guardar", class: "btn btn-dark mb-3 mt-3" %>
      <% end %>
    </div>
    <%= link_to 'Regresar a Carrera', career_path(@career), class: "btn btn-ghost m-5" %>
  </div>
</div>
